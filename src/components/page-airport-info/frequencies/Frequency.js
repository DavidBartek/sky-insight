export const Frequency = ({freqObj}) => {
    
    // This module is basically going to have a ton of conditional statements as handlers.

    // FILTER BY CIVILIAN FREQ RANGE (118.0 - 136.975) (outside: return "")

    // Weather

    // Ground

    // Tower

    // Clearance
    
    return <>{freqObj.FREQ_USE}</>
}